# ADR 001: Local-only MVP

The MVP runs entirely in the browser. Content is bundled and progress is stored in LocalStorage. Authentication, servers, external APIs, and deployment configuration are excluded.

